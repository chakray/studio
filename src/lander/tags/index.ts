import { HeroTag } from './hero/hero.tag';
import { ListAsideTag } from './list-aside/list-aside.tag';
import { HsplitTag } from './hsplit/hsplit.tag';
import { FooterTag } from './footer/footer.tag';

export const tagList = [
  HeroTag, ListAsideTag, HsplitTag, FooterTag
];
export const tags = {
  HeroTag, ListAsideTag, HsplitTag, FooterTag
};
